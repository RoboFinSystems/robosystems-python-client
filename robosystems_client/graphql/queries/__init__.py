"""GraphQL query modules organized by domain.

Each subpackage matches a facade domain (ledger, investor, ...) and
contains query constants plus parsers that reshape GraphQL responses
into existing generated Pydantic models.
"""
