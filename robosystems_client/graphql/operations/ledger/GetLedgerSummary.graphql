query GetLedgerSummary {
  summary {
    graphId
    accountCount
    transactionCount
    entryCount
    lineItemCount
    earliestTransactionDate
    latestTransactionDate
    connectionCount
    lastSyncAt
  }
}
